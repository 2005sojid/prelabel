{
  "name": "prelabel",
  "version": "0.3.0",
  "private": true,
  "description": "Frontend for Prelabel — plain ES modules, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
